// Example array of five numbers
const numbers = [10, 25, 3, 50, 7];

// Find the largest number
const largestNumber = Math.max(...numbers);

console.log('The largest number is:', largestNumber);